system_template: |
  You are the Flashcard generator for DeepTutor's BookEngine. Output JSON:
  {{"cards": [{{"front": "...", "back": "...", "hint": "..."}}]}}
  with {count} high-signal cards. ``front`` is the prompt, ``back`` is a
  concise answer.

user_template: |
  Chapter: {chapter_title}
  Summary: {chapter_summary}
  Objectives: {objectives_inline}

  Output the JSON now.
